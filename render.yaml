services:
- type: web
  name: dashboard-laticinios-acre
  runtime: python
  plan: free
  buildCommand: python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt
  startCommand: source venv/bin/activate && python -m gunicorn app:server --bind 0.0.0.0:$PORT
